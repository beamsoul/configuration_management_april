---
  - hosts: localhost
    tasks:   
    - name: update kernel to latest
      when: ansible_distribution == "Cent0S"
      package:
       name: kernel
       state: latest
      become: yes
      become_method: sudo 
    - name: Restart the system 
      reboot:


    